# NewsGap 快速启动指南

## 5 分钟快速体验

### 第一步：启动后端

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（首次）
python -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows

# 安装依赖（首次）
pip install -r requirements.txt

# 启动后端服务
python main.py
```

后端将在 `http://localhost:8000` 运行

### 第二步：启动前端

打开**新的终端窗口**：

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次）
npm install

# 启动开发服务器
npm run dev
```

前端将在 `http://localhost:5173` 运行

### 第三步：配置 LLM（可选）

**如果使用本地 Ollama**：

```bash
# 安装 Ollama
curl -fsSL https://ollama.com/install.sh | sh

# 下载模型
ollama pull llama3.1
```

**如果使用 DeepSeek API**：

```bash
# 配置 API Key
export DEEPSEEK_API_KEY="sk-your-api-key"
```

### 第四步：开始使用

1. 打开浏览器访问 `http://localhost:5173`
2. 在首页选择行业（如"AI"）
3. 点击"一键情报"（或"仅爬取"）
4. 等待处理完成
5. 查看文章列表和分析结果

## 项目结构

```
NewsGap/
├── backend/          # Python 后端
│   ├── crawler/      # 爬虫模块
│   ├── storage/      # 存储模块
│   ├── llm/          # LLM 适配器
│   ├── routes/       # API 路由
│   ├── main.py       # 入口文件
│   └── config.yaml   # 配置文件
│
├── frontend/         # React 前端
│   ├── src/
│   │   ├── pages/    # 页面组件
│   │   └── services/ # API 客户端
│   └── package.json
│
├── docs/             # 文档
│   ├── architecture.md
│   ├── api.md
│   └── llm-integration.md
│
└── README.md
```

## 常见问题

### Q: 后端启动失败？

**A**: 检查端口占用

```bash
# macOS/Linux
lsof -i :8000

# Windows
netstat -ano | findstr :8000
```

### Q: 前端无法连接后端？

**A**: 确保后端已启动，并检查 `frontend/vite.config.ts` 中的 proxy 配置。

### Q: 爬取失败？

**A**: 
1. 检查网络连接
2. 确认信息源 URL 有效
3. 查看后端日志

### Q: LLM 分析失败？

**A**:
1. 检查 API Key 是否正确配置
2. 确认 Ollama 服务是否运行（如使用本地模型）
3. 检查网络连接（如使用 API）

## 下一步

- 📖 阅读 [完整文档](README.md)
- 🏗️ 了解 [架构设计](docs/architecture.md)
- 🔌 查看 [API 文档](docs/api.md)
- 🤖 配置 [LLM 后端](docs/llm-integration.md)

## 停止服务

### 停止后端

在后端终端按 `Ctrl+C`

### 停止前端

在前端终端按 `Ctrl+C`

## 生产部署

### 后端

```bash
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 前端

```bash
cd frontend
npm run build
# 静态文件在 dist/ 目录
```

---

**提示**：首次运行时，后端会自动创建数据库并插入示例信息源。

**需要帮助？** 查看 [常见问题](README.md) 或提交 [Issue](https://github.com/your-repo/issues)。
